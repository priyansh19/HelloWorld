# Runtime dependencies for the MemGraph widget.
# The graph is hand-painted with QPainter, so no charting lib is needed.
PySide6>=6.6
psutil>=5.9
# NVIDIA VRAM support (optional at runtime; import name is `pynvml`).
# On non-NVIDIA machines the widget degrades gracefully and simply hides VRAM.
nvidia-ml-py>=12.535
